#!/bin/bash

function getMonitor() {
  c-list-all-displays | rofi -dmenu -p "$1"
}

monitor1=`getMonitor "Monitor 1"`
monitor2=`getMonitor "Monitor 2"`

echo $monitor1 > $HOME/bin/runtime/monitor1
echo $monitor2 > $HOME/bin/runtime/monitor2

